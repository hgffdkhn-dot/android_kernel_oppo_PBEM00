name: Build R17 Kernel

on:
  workflow_dispatch:
    inputs:
      mode:
        description: '编译模式（先跑 stock 验证开机）'
        required: true
        default: 'stock'
        type: choice
        options:
          - stock
          - ksunext

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 300

    steps:
      - name: 检出内核源码
        uses: actions/checkout@v4
        with:
          ref: ten
          fetch-depth: 1

      - name: 安装依赖
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            bc bison flex libssl-dev libelf-dev ccache \
            make gcc device-tree-compiler zip curl \
            python3 python-is-python3
          # 工具链里的 gcc 是 #!/usr/bin/python 包装脚本，
          # Ubuntu 22.04 默认没有 /usr/bin/python，必须装 python-is-python3

      - name: 下载 GCC 4.9 工具链
        run: |
          set -e
          rm -rf "$GITHUB_WORKSPACE/tc"
          mkdir -p "$GITHUB_WORKSPACE/tc"
          echo "尝试主源: LineageOS 镜像"
          if git clone --depth 1 -b lineage-19.1 \
              https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git \
              "$GITHUB_WORKSPACE/tc" ; then
            echo "[OK] 主源下载成功"
          else
            echo "[!] 主源失败，回退 AOSP"
            rm -rf "$GITHUB_WORKSPACE/tc"
            mkdir -p "$GITHUB_WORKSPACE/tc"
            git clone --depth 1 \
              https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 \
              "$GITHUB_WORKSPACE/tc"
          fi

      - name: 校验工具链（缺失立刻失败，不再假绿）
        run: |
          set -e
          export CROSS_COMPILE=$GITHUB_WORKSPACE/tc/bin/aarch64-linux-android-
          ls -la "$GITHUB_WORKSPACE/tc/bin/" | head -20
          if [ ! -f "${CROSS_COMPILE}gcc" ]; then
            echo "::error::工具链缺失: ${CROSS_COMPILE}gcc"
            exit 1
          fi
          chmod +x "$GITHUB_WORKSPACE/tc/bin/"*
          "${CROSS_COMPILE}gcc" --version || {
            echo "::error::gcc 无法执行（可能缺少 /usr/bin/python）"
            exit 1
          }

      - name: 修复蓝牙 btfm_slim 编译
        run: |
          set -e
          echo "=== 根因：btfm_slim.c 用 #include <btfm_slim.h>（尖括号），"
          echo "    但 drivers/bluetooth/Makefile 没有 -I 指向自身目录，gcc 找不到。"
          echo "    头文件其实一直在仓库里，缺的是 include 搜索路径。"

          # 1) 确保头文件在位（从本仓库 ksu-build/ 兜底，不依赖外网）
          for h in btfm_slim.h btfm_slim_wcn3990.h; do
            if [ -f "drivers/bluetooth/$h" ]; then
              echo "[=] $h 已存在"
            else
              cp "ksu-build/$h" "drivers/bluetooth/$h" && echo "[+] 已补齐 $h"
            fi
          done

          # 2) 给 Makefile 加 include 搜索路径（真正的关键修复）
          MK=drivers/bluetooth/Makefile
          if grep -q "^-I\$(src)" "$MK"; then
            echo "[=] $MK 已有 -I\$(src)"
          else
            cp "$MK" "$MK.bak"
            sed -i '/^ccflags-y/a ccflags-y += -I$(src)' "$MK"
            echo "[+] 已插入 ccflags-y += -I\$(src)"
          fi
          echo "--- 当前 ccflags 行:"
          grep -n "ccflags" "$MK"

      - name: 移除 R17 不存在的 NXP 功放 (tfa98xx)
        run: |
          set -e
          # R17 出厂配置无 NXP 功放，但 techpack Makefile 硬编码编它，
          # 且它带 GCC4.9 不认识的 -Wno-enum-conversion，被 -Werror 放大成错。
          echo "--- 处理前的引用:"
          grep -rn "tfa98xx" techpack/audio --include=Makefile* || true
          find techpack -name 'Makefile*' -exec sed -i '/[tT][fF][aA]98/d' {} + || true
          echo "--- 处理后:"
          grep -rn "tfa98xx" techpack/audio --include=Makefile* || echo "(已清空)"

      - name: 移除 GCC 4.9 不支持的编译选项
        run: |
          set -e
          before=$(grep -rl "Wno-enum-conversion" techpack kernel drivers 2>/dev/null | wc -l)
          echo "命中文件数: $before"
          grep -rl "Wno-enum-conversion" techpack kernel drivers 2>/dev/null \
            | xargs -r sed -i 's/-Wno-enum-conversion//g' || true
          echo "[OK] 已清除"

      - name: 应用 defconfig
        run: |
          set -e
          cp arch/arm64/configs/sdm670_r17_ksu_defconfig .config
          grep -E "CONFIG_KSU=|CONFIG_OPPO_ROOT_CHECK|CONFIG_BTFM_SLIM=|TFA98XX" .config || true

      - name: 解析依赖
        run: |
          set -e
          export ARCH=arm64 SUBARCH=arm64
          export CROSS_COMPILE=$GITHUB_WORKSPACE/tc/bin/aarch64-linux-android-
          make olddefconfig

      # ================= 仅 ksunext 模式 =================
      - name: "[KSU] 拉取 Reno 4.9 backport"
        if: inputs.mode == 'ksunext'
        run: |
          git config user.email ci@example.com
          git config user.name CI
          git remote add reno https://github.com/OP46B1-Dev/android_kernel_oppo_sdm710.git
          git fetch --depth 1 reno deprecated/color-11.1_rksu
          for sha in \
            ba18867fe84e9e41d13535eee933a9f5e733babf \
            7e103d9bc84bbb791b65b15ad7c9b62d3023eb0e \
            782269366a1928466ba8aa4f0bffc88da861d866 \
            be3865c776a668bc914976b6c5f42fc8b1fe8bd1 \
            3460c15b1b937b7a8b256c1f2982debc10bf1301 \
            38baa0988a7aeef2efbfb4ccf6b06dd8d50e0538 \
            ba688d384ae45185e509d4152c5c86b82b1011bb ; do
            echo "--- cherry-pick $sha"
            git cherry-pick --no-commit "$sha" || echo "[!] 冲突 $sha"
          done
          git status --short

      - name: "[KSU] 安装 KernelSU-Next"
        if: inputs.mode == 'ksunext'
        run: |
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/legacy/kernel/setup.sh" | bash -s legacy
          ls drivers/kernelsu || echo "[!] 缺失"

      - name: "[KSU] 注入 hook"
        if: inputs.mode == 'ksunext'
        run: python3 ksu-build/inject_hooks.py .

      # ================= 编译 =================
      - name: 编译内核
        run: |
          export ARCH=arm64 SUBARCH=arm64
          export CROSS_COMPILE=$GITHUB_WORKSPACE/tc/bin/aarch64-linux-android-
          export USE_CCACHE=1 CCACHE_DIR=$GITHUB_WORKSPACE/.ccache
          ret=0
          make -j$(nproc) > build.log 2>&1 || ret=$?
          echo "===== 最后 100 行 ====="
          tail -100 build.log
          echo "===== make exit=$ret ====="
          if [ $ret -ne 0 ]; then
            echo "== 错误上下文 =="
            grep -nE "error:|Error [0-9]|No such file" build.log | head -40
            exit $ret
          fi

      - name: 收集产物
        run: |
          set -e
          mkdir -p out
          ok=0
          for f in arch/arm64/boot/Image.gz-dtb \
                   arch/arm64/boot/Image.gz \
                   arch/arm64/boot/Image-dtb \
                   arch/arm64/boot/Image ; do
            if [ -f "$f" ]; then cp "$f" out/; echo "[+] 已收集 $f"; ok=1; fi
          done
          cp .config out/config.txt
          [ -f build.log ] && cp build.log out/build.log || true
          ls -la out/
          if [ "$ok" -ne 1 ]; then
            echo "::error::未生成内核镜像，编译未成功"
            exit 1
          fi

      - name: 上传
        uses: actions/upload-artifact@v4
        with:
          name: r17-${{ inputs.mode }}
          path: out/
          retention-days: 30
